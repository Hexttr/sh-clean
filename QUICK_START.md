# Быстрый старт Shannon на сервере

## ✅ Статус

Shannon **уже установлен** на сервере в `/root/shannon`!

## 🚀 Запуск

### 1. Подключитесь к серверу
```bash
ssh root@72.56.79.153
```

### 2. Перейдите в директорию Shannon
```bash
cd /root/shannon
```

### 3. Настройте API ключ (если еще не настроен)
```bash
nano .env
# Добавьте или обновите:
# ANTHROPIC_API_KEY=ваш-ключ-от-anthropic
```

### 4. Запустите Shannon

**Пример 1: Тестирование Juice Shop (уже запущен на порту 3001)**
```bash
./shannon start URL=http://localhost:3001 REPO=/root/shannon
```

**Пример 2: Тестирование внешнего приложения**
```bash
./shannon start URL=https://example.com REPO=/path/to/your/repo
```

**Пример 3: С конфигурационным файлом**
```bash
./shannon start URL=https://example.com REPO=/path/to/repo CONFIG=./configs/example-config.yaml
```

## 📊 Мониторинг

### Просмотр логов
```bash
./shannon logs ID=<workflow-id>
```

### Проверка статуса
```bash
./shannon query ID=<workflow-id>
```

### Веб-интерфейс Temporal
Откройте в браузере: **http://72.56.79.153:8233**

## 🛑 Остановка

```bash
# Остановить контейнеры (сохранить данные)
./shannon stop

# Полная очистка
./shannon stop CLEAN=true
```

## 📁 Где найти результаты

Результаты сохраняются в:
```
/root/shannon/audit-logs/{hostname}_{sessionId}/
```

Внутри директории:
- `comprehensive_security_assessment_report.md` - финальный отчет
- `session.json` - метрики и данные сессии
- `agents/` - логи выполнения агентов
- `prompts/` - снимки промптов

## ⚙️ Полезные команды

### Проверка статуса контейнеров
```bash
docker ps
```

### Просмотр логов контейнеров
```bash
docker compose logs -f worker
docker compose logs -f temporal
```

### Перезапуск контейнеров
```bash
docker compose restart
```

### Проверка портов
```bash
ss -tuln | grep -E ':(7233|8233)'
```

## 📝 Важные заметки

1. **API ключ обязателен** - без него Shannon не запустится
2. **Время выполнения** - полный пентест занимает 1-1.5 часа
3. **Стоимость** - примерно $50 USD за полный тест (зависит от модели Claude)
4. **Порты**:
   - 7233 - Temporal gRPC (внутренний)
   - 8233 - Temporal Web UI (доступен извне)
5. **Juice Shop** - уже запущен на порту 3001 для тестирования

## 🔗 Полезные ссылки

- [Официальная документация Shannon](https://github.com/KeygraphHQ/shannon)
- [Примеры конфигураций](https://github.com/KeygraphHQ/shannon/tree/main/configs)
- [Примеры отчетов](https://github.com/KeygraphHQ/shannon/tree/main/sample-reports)

## ❓ Помощь

Если что-то не работает:
1. Проверьте `.env` файл - есть ли API ключ
2. Проверьте логи: `docker compose logs`
3. Проверьте статус контейнеров: `docker ps`
4. Убедитесь, что порты 7233 и 8233 свободны

