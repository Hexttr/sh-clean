# Руководство по развертыванию Shannon на сервере

## ✅ Что уже сделано

1. ✅ Репозиторий Shannon клонирован на сервер в `/root/shannon`
2. ✅ Создан базовый `.env` файл
3. ✅ Скрипт `shannon` настроен с правами на выполнение
4. ✅ Сервер подготовлен (Docker установлен)

## 📋 Следующие шаги

### 1. Настройка API ключа

**Вариант A: Через скрипт (рекомендуется)**
```bash
python setup_env_on_server.py
```

**Вариант B: Вручную через SSH**
```bash
ssh root@72.56.79.153
cd /root/shannon
nano .env
# Добавьте: ANTHROPIC_API_KEY=ваш-ключ
```

### 2. Проверка Docker Compose

Shannon использует `docker compose` (плагин Docker) или `docker-compose` (отдельная утилита).

Проверьте на сервере:
```bash
ssh root@72.56.79.153
docker compose version
# или
docker-compose --version
```

Если не установлен, установите:
```bash
# Для docker compose (плагин) - обычно уже включен в Docker 20.10+
# Для docker-compose (отдельная утилита):
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose
```

### 3. Запуск Shannon

**Базовый запуск:**
```bash
ssh root@72.56.79.153
cd /root/shannon
./shannon start URL=https://your-app.com REPO=/path/to/repo
```

**Пример с тестовым приложением (Juice Shop уже запущен на порту 3001):**
```bash
cd /root/shannon
./shannon start URL=http://localhost:3001 REPO=/root/shannon/sample-reports
```

**С конфигурационным файлом:**
```bash
./shannon start URL=https://your-app.com REPO=/path/to/repo CONFIG=./configs/example-config.yaml
```

### 4. Мониторинг

**Просмотр логов:**
```bash
./shannon logs ID=<workflow-id>
```

**Запрос статуса:**
```bash
./shannon query ID=<workflow-id>
```

**Веб-интерфейс Temporal:**
Откройте в браузере: `http://72.56.79.153:8233`

### 5. Остановка

**Остановить контейнеры (сохранить данные):**
```bash
./shannon stop
```

**Полная очистка (удалить все данные):**
```bash
./shannon stop CLEAN=true
```

## 🔧 Структура на сервере

```
/root/shannon/
├── .env                    # Конфигурация (нужно добавить API ключ)
├── shannon                 # CLI скрипт
├── docker-compose.yml      # Конфигурация Docker
├── Dockerfile             # Образ для worker
├── package.json           # Node.js зависимости
├── src/                   # Исходный код
├── prompts/               # Промпты для AI
├── configs/               # Конфигурационные файлы
└── audit-logs/            # Результаты пентестов
```

## 📝 Важные замечания

1. **API ключ обязателен**: Без `ANTHROPIC_API_KEY` Shannon не запустится
2. **Порты**: 
   - 7233 - Temporal gRPC (внутренний)
   - 8233 - Temporal Web UI (доступен извне)
3. **Результаты**: Сохраняются в `./audit-logs/{hostname}_{sessionId}/`
4. **Время выполнения**: Полный пентест занимает 1-1.5 часа
5. **Стоимость**: Примерно $50 USD за полный тест (зависит от модели)

## 🚀 Быстрый старт

```bash
# 1. Настройте API ключ
python setup_env_on_server.py

# 2. Подключитесь к серверу
ssh root@72.56.79.153

# 3. Запустите Shannon
cd /root/shannon
./shannon start URL=http://localhost:3001 REPO=/root/shannon

# 4. Откройте Temporal UI
# http://72.56.79.153:8233
```

## 📚 Дополнительная информация

- [Официальная документация Shannon](https://github.com/KeygraphHQ/shannon)
- [Примеры конфигураций](./configs/example-config.yaml)
- [Примеры отчетов](./sample-reports/)

